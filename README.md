# Setup

* Clone the repository

```bash
git clone ___
```

* Install Gem dependencies

```bash
bundle install --binstubs
```

* Run code

```
ruby dog.rb
```

* Run RSpec tests

```bash
bin/rspec
```